# Terraform module contract

Reusable modules should:
- encode secure defaults
- expose narrow inputs
- produce useful outputs
- support versioning
- use remote state + locking
- tag ownership and cost
- include observability defaults
- avoid public data services
- support policy checks in CI
